#include<stdio.h>
#include<string.h>
main()
{
char one[10], two[10];
int a;
printf("Enter a string");
scanf("%s",one);
strcpy(two,one);
strrev(one);

a = strcmp(two,one);
if (a==0)
{
printf("no is a palindrome");
}
{
printf("its not a palindrome");
}}
